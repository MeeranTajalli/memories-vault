{
  "name": "memories-app",
  "version": "1.0.0",
  "description": "A web app to upload and view images in Azure Blob Storage.",
  "main": "server.js",
  "engines": {
    "node": "18.x"  
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for Node.js backend'" 
  },
  "dependencies": {
    "@azure/storage-blob": "^12.10.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "multer": "^1.4.5"
  }
}
